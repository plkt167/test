drop database if exists bbs;
create database bbs;
use bbs;
drop table if exists user;
CREATE TABLE user (
    userid VARCHAR(20),
    userpassword VARCHAR(20),
    username VARCHAR(20),
    usergender VARCHAR(20),
    useremail VARCHAR(50), 
    primary key (userid));
    

select * from user;
    
    
    drop table if exists bbs;
    create table bbs(bbsid int, bbstitle varchar(50),
    userid varchar(20), bbsdate datetime, bbscontent varchar(2048),
    bbsavailable int,
    primary key (bbsid));
    
    select * from bbs;
    delete from bbs;
